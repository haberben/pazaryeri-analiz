name: Code checks
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check live.js
          node --check excel-import.js
          node --check smart-analysis.js
          node --check api/price.js
          node --check api/search.js
      - name: Run price/parser unit tests
        run: node --test tests/*.test.js
